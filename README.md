# Arkane

## Keypoints

* Asset is always represented by a file in the vault.
* Philosophy of extension: everything should represent an asset. For example - extension `score` - still works with assets under the hood.
* Every extension module has own named file-extension.
